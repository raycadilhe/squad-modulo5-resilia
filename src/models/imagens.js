class Imagens {
    constructor( id, titulo, descricao, url ){
        this.id = id,
        this.titulo = titulo,
        this.descricao = descricao,
        this.url = url
    }
}

export default Imagens